mode: ContinuousDeployment
next-version: 1.0.0
branches:
  main:
    regex: ^main$
    mode: ContinuousDeployment
    label: ''
    increment: Patch
    track-merge-target: false
    source-branches: []
    is-mainline: true
  feature:
    regex: ^features?[/-]
    mode: ContinuousDeployment
    label: alpha
    increment: Minor
    source-branches:
      - main
  release:
    regex: ^releases?[/-]
    mode: ContinuousDeployment
    label: rc
    increment: None
    source-branches:
      - main
  hotfix:
    regex: ^hotfix(es)?[/-]
    mode: ContinuousDeployment
    label: hotfix
    increment: Patch
    source-branches:
      - main
      - master
      - release
ignore:
  sha: []
